# Webpage for creating journals

### SETUP & RUN
* `npm i`
* Create a database by typing `CREATE DATABASE journals` in your CLI
* Run `knex migrate:latest`
* `npm start`
* Visit `http://localhost:3000/`
